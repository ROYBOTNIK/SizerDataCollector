<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <!-- These are placeholders for now. We'll use them later with WCF. -->
    <add key="SizerHost" value="10.155.155.10" />
    <add key="SizerPort" value="8001" />
    <!-- Timeouts in seconds -->
    <add key="OpenTimeoutSec" value="5" />
    <add key="SendTimeoutSec" value="5" />
    <add key="ReceiveTimeoutSec" value="5" />
  </appSettings>

  <!-- WCF settings will be generated here *later* when you add the Service Reference. -->
  <system.serviceModel>
    <bindings>
      <!-- placeholder; VS will likely overwrite/extend this section -->
    </bindings>
    <client>
      <!-- placeholder -->
    </client>
  </system.serviceModel>
</configuration>
